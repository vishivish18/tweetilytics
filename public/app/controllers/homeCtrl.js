angular.module('app')
    .controller('homeCtrl', function($scope, $http) {
        $scope.setup = function() {
           // use data mutator to act upon data
           
        };
        $scope.setup();
    });
